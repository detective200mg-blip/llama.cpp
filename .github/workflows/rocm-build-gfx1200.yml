name: ROCM Windows gfx1200 Build
on: [workflow_dispatch]
jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    - name: Setup Build Environment
      run: |
        choco install cmake git
        echo "Setting up ROCM 6.4 for Windows - placeholder"
    - name: Build Windows DLL
      run: |
        cd llama.cpp
        echo "Actual build commands for Windows ROCM DLL targeting gfx1200 would go here"
        # This is where we'd put the real build commands
