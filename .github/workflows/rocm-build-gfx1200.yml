name: ROCM Windows gfx1200
on: [workflow_dispatch]
jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    - name: Setup ROCM
      run: |
        # Install ROCM for Windows - this is placeholder
        echo "Install ROCM 6.4 for Windows"
    - name: Build
      run: |
        # Build Windows DLL targeting gfx1200
        echo "Build commands for Windows ROCM DLL"
